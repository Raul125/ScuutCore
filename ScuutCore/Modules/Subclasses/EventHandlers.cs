using Exiled.Events.EventArgs;

namespace ScuutCore.Modules.Subclasses
{
    public class EventHandlers
    {
        private Subclasses subclasses;
        public EventHandlers(Subclasses subclasses)
        {
            this.subclasses = subclasses;
        }
    }
}